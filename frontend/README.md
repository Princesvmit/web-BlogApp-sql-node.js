# Blog Frontend

This is a minimal React frontend for the blog app. Install dependencies with `npm install` and start with `npm start`.

It expects the backend API to be available at `http://localhost:4000` by default.
